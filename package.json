{
  "dependencies": {
    "sass": "^1.17.3",
    "stylelint": "^9.10.1",
    "stylelint-config-rational-order": "^0.0.4"
  },
  "scripts": {
    "build": "sass style:build",
    "build:dev": "sass --watch style:build",
    "lint": "stylelint style/*"
  }
}
